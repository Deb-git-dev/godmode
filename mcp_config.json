{
  "$schema": "https://json.schemastore.org/mcp-config.json",
  "mcpServers": {
    "supabase": {
      "type": "remote",
      "url": "https://mcp.supabase.com/mcp",
      "description": "Hosted Supabase MCP for schema inspection, database migrations, and queries",
      "headers": {
        "Authorization": "Bearer ${SUPABASE_ACCESS_TOKEN}"
      }
    },
    "github": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "Official GitHub MCP server for repo, PR, and issue management"
    },
    "gitmcp": {
      "type": "remote",
      "url": "https://gitmcp.io/api",
      "description": "Remote GitMCP for grounding on public GitHub repositories without local clones"
    },
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@executeautomation/playwright-mcp-server"],
      "description": "Browser automation and UI visual testing server"
    },
    "canva": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "canva-mcp"],
      "env": {
        "CANVA_API_KEY": "${CANVA_API_KEY}"
      },
      "description": "Canva asset generation & design management MCP server (registry.npmjs.org/canva-mcp)"
    },
    "asana": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-asana"],
      "env": {
        "ASANA_ACCESS_TOKEN": "${ASANA_ACCESS_TOKEN}"
      },
      "description": "Asana task and project tracking MCP server"
    },
    "headroom": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "headroom-ai", "mcp"],
      "description": "Headroom CCR context compression, retrieval, and token savings MCP server"
    }
  }
}
