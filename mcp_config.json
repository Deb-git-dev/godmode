{
  "$schema": "https://json.schemastore.org/mcp-config.json",
  "mcpServers": {
    "supabase": {
      "type": "remote",
      "url": "https://mcp.supabase.com/mcp",
      "description": "Hosted Supabase MCP for schema inspection, database migrations, and queries",
      "headers": {
        "Authorization": "Bearer ${SUPABASE_ACCESS_TOKEN}"
      }
    },
    "github": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "Official GitHub MCP server for repo, PR, and issue management"
    },
    "gitmcp": {
      "type": "remote",
      "url": "https://gitmcp.io/api",
      "description": "Remote GitMCP for grounding on public GitHub repositories without local clones"
    },
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@executeautomation/playwright-mcp-server"
      ],
      "description": "Browser automation and UI visual testing server"
    },
    "canva": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "canva-mcp"
      ],
      "env": {
        "CANVA_API_KEY": "${CANVA_API_KEY}"
      },
      "description": "Canva asset generation & design management MCP server (registry.npmjs.org/canva-mcp)"
    },
    "asana": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-asana"
      ],
      "env": {
        "ASANA_ACCESS_TOKEN": "${ASANA_ACCESS_TOKEN}"
      },
      "description": "Asana task and project tracking MCP server"
    },
    "headroom": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "headroom-ai",
        "mcp"
      ],
      "description": "Headroom CCR context compression, retrieval, and token savings MCP server"
    },
    "firecrawl": {
      "type": "remote",
      "url": "https://mcp.firecrawl.dev/v2/mcp",
      "headers": {
        "Authorization": "Bearer fc-142efcf8619f4221b6b43495bf2b6dd0"
      },
      "description": "Official Firecrawl MCP server for web search, scrape, crawl, and LLM-ready markdown extraction"
    },
    "shadcn-ui": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@jpisnice/shadcn-ui-mcp-server"
      ],
      "description": "shadcn/ui MCP server providing component structure, registry blocks, and installation context (Jpisnice/shadcn-ui-mcp-server)"
    },
    "magic-mcp": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@21st-dev/magic-mcp"
      ],
      "description": "21st.dev Magic MCP server for AI component generation and UI catalog discovery (21st-dev/magic-mcp)"
    }
  }
}